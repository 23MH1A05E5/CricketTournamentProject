apiVersion: v1
kind: Service
metadata:
  name: springboot-openshift-service
spec:
  selector:
    app: springboot-openshift
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
  type: NodePort